# Maximum-Clique-Detection-in-a-Graph-Using-MIPS-Assembly
MIPS Assembly program to detect the maximum clique in a small undirected graph (≤5 vertices) using a brute-force approach. Reads an adjacency matrix from a file, finds the largest fully connected vertex subset, and writes the result to an output file.
